# Spec evaluation: query-api

| Axis | Score | Evidence | Gap to next level |
|------|-------|----------|-------------------|
| Testability of acceptance criteria | 3/3 | `requirements.md:26` "EARS form: `WHEN <trigger> THEN <observable outcome>`"; `requirements.md:29-64` every AC phrased `WHEN ... THEN ...` with binary-checkable outcome; edge cases enumerated inline — boundary inclusivity (`AC-F4`, `AC-F5`), limit clamp (`AC-P7`), cursor mismatch (`AC-E9`, `AC-E10`), multi-actor dedup/order/cap (`AC-F1b`, `AC-F1c`, `AC-F1d`, `AC-E11`), resource escape (`AC-X1`–`AC-X3`). | Already production-grade. |
| Determinism of design              | 3/3 | Locked: `design.md:15` sort `(timestamp, id)`; `design.md:60-64` keyset SQL; `design.md:118-130` 400 cases parse vs semantic; `design.md:138-147` four composite indexes with p95 justification; `design.md:95-96` µs truncation. Multi-actor now fully covered: `design.md:10` param row `comma-separated, ≤10`; `design.md:98-110` `### Multi-actor filter` defines normalization pipeline (split → trim → dedup → `String.compareTo` sort), cap-10 → 400, `actor = ANY(:actors)` SQL, BitmapOr index path on existing `(actor, timestamp DESC, id DESC)`; `design.md:72-91` `##### Filter hash canonical form` specifies field order, `\n` separator, byte-level serialization rules, with three example cases (sort-equivalent, dedup-equivalent, case-sensitive divergent). | Already production-grade. |
| Decomposition quality of tasks     | 2/3 | `tasks.md:3` "One task = one safe commit"; per-task `Refs`, `Depends on`, `Scope`, `DoD`, `Repo status`; `tasks.md:228-236` dependency graph proves order. Gap: multi-actor ACs (`AC-F1a`–`AC-F1d`, `AC-E11`) have no owning task — task 2 refs `AC-F1..AC-F7`, task 4 refs `AC-E1..AC-E7, AC-P7, AC-P8`, task 8 refs `AC-F1..AC-F7`, task 10 refs `AC-E1..AC-E10, AC-P7`. All 12 tasks marked `[x]` done, so the multi-actor work is unrepresented in the task plan. | Add task 13 "Extend `actor` filter to comma-separated multi-value" covering: parse + normalize per `design.md#multi-actor-filter`, repository switch to `actor = ANY(:actors)`, validator for `>10` distinct → 400, cursor filter-hash input switched to canonical actor-set serialization per `design.md#filter-hash-canonical-form`. Include explicit `Refs: AC-F1a..F1d, AC-E11` and `Depends on: 2, 3, 4`. Alternative: reopen tasks 2/4/8/10 with `[~]` and extended `Refs` — pick one path. |

**Verdict:** SHIP
**Reason:** All three axes ≥ 2 (3 / 3 / 2). Testability and determinism are production-grade after EARS rewrite and multi-actor design sync; decomposition still meets the bar but the multi-actor extension lacks an owning task.

**Optional improvements to reach 3/3/3:**
1. `tasks.md` — add task 13 per gap above linking `AC-F1a..F1d` and `AC-E11`; update dependency graph (`13` between `4` and `8/10`). Or reopen tasks 2/4/8/10 with `[~]` and extend each `Refs` list — single path, no orphan ACs.
2. `requirements.md` — small consistency pass: `### Multi-actor semantics` lives at the bottom as prose; promote the normalization rule into an explicit AC (`AC-F1e WHEN actor list contains blanks/dupes/different order THEN effective filter and cursor hash are identical to the sorted-deduped form`) so cursor-identity contract is verifiable from the AC list alone.
3. `tasks.md` — once task 13 lands (or 2/4/8/10 reopened), flip the 12 `[x]` markers to reflect actual repo state; the current spec asserts "done" for ACs that have no implementation evidence (`AC-F1a..d`, `AC-E11`) — risks misleading future readers.
